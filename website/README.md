# Lexino AI Website

This folder is the safe Next.js frontend migration target.

Run locally from this folder:

```bash
npm install
npm run dev
```

Vercel should use this folder as the project root once the migration is verified.
